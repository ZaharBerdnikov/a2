{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "playwright": {
      "type": "local",
      "command": ["docker", "exec", "-i", "-e", "PLAYWRIGHT_CHROMIUM_SANDBOX=0", "-e", "PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1", "mcp-playwright", "playwright-mcp", "--headless", "--no-sandbox", "--executable-path", "/ms-playwright/chromium-1212/chrome-linux64/chrome"],
      "enabled": true
    },
    "postgres": {
      "type": "local",
      "command": ["docker", "exec", "-i", "mcp-postgres", "mcp-server-postgres", "postgresql://n8n:n8n_password@pgvector:5432/n8n_vectors"],
      "enabled": true
    },
    "n8n": {
      "type": "local",
      "command": ["docker", "exec", "-i", "mcp-n8n", "pl-n8n-mcp"],
      "environment": {
        "N8N_API_URL": "http://host.docker.internal:5678/api/v1"
      },
      "enabled": true
    }
  }
}
